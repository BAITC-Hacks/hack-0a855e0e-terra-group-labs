# Codex Implementer Prompt

Read `HACKATHON_CONTEXT.md`, `AGENTS.md`, `.agents/OWNERSHIP.md`, and `.agents/handoffs/codex.md`.

Work only on the currently assigned objective.

Requirements:
- Inspect existing code/contracts before editing.
- Implement the smallest complete solution.
- Do not add speculative features or abstractions.
- Put deterministic rules in deterministic code.
- Keep UI production-like and specific to the task; avoid generic AI-dashboard styling.
- Run the narrowest relevant validation.
- Do not claim success without observed evidence.
- Update `.agents/handoffs/codex.md` with what changed, validation, risks, and the next exact action.
